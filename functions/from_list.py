from api_search import search_book

  #status        Status    @default(TO_READ)
  #owned         Owned     @default(NO)
  #addedAt       DateTime  @default(now())
  #finishedAt    DateTime?
  #notes         String?


def search_from_list():

    return {}